# command to use:
# ansible-playbook playbooks/ubuntu.yml

- name: Initialize Ubuntu Servers
  hosts: ubuntu
  become: true
  gather_facts: true
  roles:
    - update
    - timezone
    - ssh-key
    - no-password-auth
    - disable-ipv6
    - unattended-upgrades
    - qemu-guest-agent
    - docker