- name: Ensure .bashrc contains alias
  ansible.builtin.template:
    src: .bashrc
    dest: "/home/{{ ansible_user }}/.bashrc"
    owner: "{{ ansible_user }}"
    group: "{{ ansible_user }}"
    mode: "0644"